这个节点，可以让humo模型直接图生视频，翻译一下。解压后，放到本地\ComfyUI\custom_nodes\ComfyUI-WanVideoWrapper目录下，覆盖KJ的HuMo文件夹

This node allows the HuMo model to directly generate video from an image.After decompression, place it in the local directory \ComfyUI\custom_nodes\ComfyUI-WanVideoWrapper.Overwrite KJ's HuMo folder.
